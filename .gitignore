# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables
.env*.local

# Emails écrits en mode dégradé (RESEND_API_KEY absente) — contiennent des
# liens/tokens de vérification, jamais commités.
.mail/

# Justificatifs uploadés en développement (storage.ts, fallback filesystem
# local quand BLOB_READ_WRITE_TOKEN est absent) — jamais commités.
.uploads/

# Common ignores
node_modules
.next/
.DS_Store

# Playwright (e2e, Phase 10)
test-results/
playwright-report/
blob-report/
playwright/.cache/

# TypeScript
next-env.d.ts
*.tsbuildinfo